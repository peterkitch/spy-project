"""React publish / deploy helpers (private / internal use only).

This sub-package implements operator-supervised tooling that
sits UPSTREAM of the React MVP. It does NOT add a runtime
capability to React; React still reads exactly one JSON
ranking artifact per the React Migration Declaration's
artifact-boundary rule.
"""
