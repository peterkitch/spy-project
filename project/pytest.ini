[pytest]
markers =
    slow: integration or heavy-compute tests not run by default
    production_smoke: tests that inspect real output/cache/signal_library/price_cache artifacts on the developer machine
addopts = -m "not slow and not production_smoke"
